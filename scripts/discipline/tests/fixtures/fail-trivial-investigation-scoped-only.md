# Fixture — FAIL

Trivial-investigation with `scoped-only`. Must fail.

## Trivial-investigation declaration

Category: scoped-only
Cannot produce error: Change is scoped to a single package.
Evidence: `git diff --stat` shows one package touched.
Falsification: If any consumer package is affected.
