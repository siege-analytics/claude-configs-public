# Fixture — FAIL

Trivial-investigation with `local-only` — the invented-token escape
that R5-R10 exploited. Must fail with a writing-rules:5 diagnostic
naming the four-token allowlist.

## Trivial-investigation declaration

Category: local-only
Cannot produce error: AST + string logic; no external contact.
Evidence: `git diff --stat` shows only scan_ast.py changed.
Falsification: If any external resource contacted, this declaration is wrong.
