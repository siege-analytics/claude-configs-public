// End-to-end Playwright stub for ticket #{ticket_id}, AC{ac_id}: {feature}.
//
// Generated by the scaffold hook from templates/tests/playwright-e2e.spec.ts.tmpl.
// Rendered per acceptance criterion at ticket-creation time.
//
// Contract: this test MUST fail with a message naming the AC when run
// against an unimplemented target. Implement the flow, replace the throw
// below with real Playwright assertions (expect(...).toBeVisible(), etc.).

import { test, expect } from '@playwright/test';

test('AC{ac_id}: {feature} (ticket #{ticket_id})', async ({ page }) => {
    // Navigate to the surface the AC covers. Replace with the real URL
    // once the feature exists.
    await page.goto('/');

    throw new Error(
        `AC{ac_id} for ticket #{ticket_id} ({feature}) is not implemented. ` +
        `Replace this throw with the assertion the AC's Falsifiable-by clause names.`
    );
});
