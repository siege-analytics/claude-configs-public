"""Integration test stub for ticket #{ticket_id}, AC{ac_id}: {feature}.

Generated by the scaffold hook from templates/tests/pytest-integration.py.tmpl.
Uses a real dependency (fixture below) rather than a mock. Replace the
fixture with the concrete dependency the AC calls for (DB session, HTTP
client, filesystem path, etc.), then implement the assertion.

Contract: this test MUST fail with a message naming the AC when run
against an unimplemented target.
"""

import pytest


@pytest.fixture(scope="function")
def real_dependency_for_ac{ac_id}():
    """Replace with a real dependency (DB session, HTTP client, ...) the AC needs.

    Do not mock. If a mock is unavoidable, note why in the fixture body
    and cite `[rule:writing-tests]` writing-tests:4.
    """
    raise NotImplementedError(
        "Fixture for AC{ac_id} ({feature}) not implemented. "
        "Wire the real dependency and remove this NotImplementedError."
    )


def test_ac{ac_id}_{feature}(real_dependency_for_ac{ac_id}) -> None:
    """AC{ac_id}: {feature}."""
    assert False, (
        "AC{ac_id} for ticket #{ticket_id} ({feature}) is not implemented. "
        "Replace this assert-fail with the real assertion the AC's "
        "Falsifiable-by clause names."
    )
