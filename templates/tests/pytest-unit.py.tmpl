"""Unit test stub for ticket #{ticket_id}, AC{ac_id}: {feature}.

Generated by the scaffold hook from templates/tests/pytest-unit.py.tmpl.
Rendered per acceptance criterion at ticket-creation time.

Contract: this test MUST fail with a message naming the AC when run
against an unimplemented target. Implement the function under test,
replace the assert-fail below with a real assertion, and remove this
docstring paragraph.
"""

import pytest


def test_ac{ac_id}_{feature}() -> None:
    """AC{ac_id}: {feature}."""
    assert False, (
        "AC{ac_id} for ticket #{ticket_id} ({feature}) is not implemented. "
        "Replace this assert-fail with the real assertion the AC's "
        "Falsifiable-by clause names."
    )
