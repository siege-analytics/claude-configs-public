// k6 performance scenario stub for ticket #{ticket_id}, AC{ac_id}: {feature}.
//
// Generated by the scaffold hook from templates/tests/k6-scenario.js.tmpl.
// Contract: this scenario MUST fail (via fail()) when run against an
// unimplemented target. Replace the fail with real k6 thresholds/checks
// once the feature ships.

import http from 'k6/http';
import { fail } from 'k6';

export const options = {
    scenarios: {
        ac{ac_id}_{feature}: {
            executor: 'constant-vus',
            vus: 1,
            duration: '10s',
            tags: {
                ticket: '{ticket_id}',
                ac: '{ac_id}',
                feature: '{feature}',
            },
        },
    },
    // Placeholder threshold. Replace with the SLO the AC's Falsifiable-by
    // clause names.
    thresholds: {
        'http_req_duration{ticket:{ticket_id}}': ['p(95)<500'],
    },
};

export default function () {
    // Placeholder target. Replace with the real endpoint once the feature exists.
    http.get('https://placeholder.example.invalid/ac{ac_id}');

    fail(
        `AC{ac_id} for ticket #{ticket_id} ({feature}) is not implemented. ` +
        `Replace this fail() with the checks the AC's Falsifiable-by clause names.`
    );
}
