// Vitest component stub for ticket #{ticket_id}, AC{ac_id}: {feature}.
//
// Generated by the scaffold hook from templates/tests/vitest-component.spec.ts.tmpl.
//
// Contract: this test MUST fail with a message naming the AC when run
// against an unimplemented target. Implement the component, then replace
// the expect.fail with real assertions (expect(...).toEqual(...), etc.).

import { describe, it, expect } from 'vitest';

describe('ticket #{ticket_id} / AC{ac_id}: {feature}', () => {
    it('satisfies AC{ac_id}: {feature}', () => {
        expect.fail(
            `AC{ac_id} for ticket #{ticket_id} ({feature}) is not implemented. ` +
            `Replace this expect.fail with the assertion the AC's Falsifiable-by clause names.`
        );
    });
});
