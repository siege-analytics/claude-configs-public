## Assumptions

Working as: software engineer + tech lead (skills-shelf author absorbing an external MIT-licensed agent skill).
Peer review needed from: tech lead (operator, dheerajchand, shelf maintainer).
Lead review needed from: tech lead — merge gate.
Goal source: operator request in session — "Is there a good way to incorporate these into the skills under the shelves? https://github.com/caylent/tufte-data-viz If so, please do so and credit the repo in our docs."
Plan reference: pre-author inventory (this artifact) is the plan; conditional intent ("If so, please do so") was resolved via inspection of upstream + workspace conventions.

## Pre-author inventory (applying authoring-against-state:6 to this work)

### Inputs read (step 1)
- Operator message above.
- Upstream `caylent/tufte-data-viz` repo metadata: `gh api repos/caylent/tufte-data-viz` — Python repo, agent skill (single SKILL.md + per-library rules/), 92 stars, MIT.
- Upstream `SKILL.md` (full, 233 lines): rules 1-22, library quick reference, chart-type guidance, color reference, anti-pattern detection, validation checklist.
- Upstream `rules/` directory listing (10 files: chartjs, echarts, matplotlib, plotly, recharts, svg-html, typography-and-color, interactive-and-accessible, anti-patterns, small-multiples-sparklines).
- Upstream `LICENSE` (MIT, © 2026 Caylent).
- Upstream HEAD commit: `ae7ca0de7819db83241b24a2618810d5f1171145` (2026-02-19, verified via `gh api repos/caylent/tufte-data-viz/commits/main`).
- Workspace `skills/shelves/AUTHORING.md` (book-skill format conventions: frontmatter, body sections, router updates, source attribution).
- Workspace `skills/shelves/storytelling/SKILL.md` (existing shelf router — current trigger table, [skill:foo] link format on develop branch).
- Workspace `skills/shelves/storytelling/storytelling-with-data/SKILL.md` head (existing data-viz entry — needed for disambiguation framing + depth reference).
- Workspace `THIRD_PARTY_NOTICES.md` (upstream sources block + per-skill attribution table format).

### Knowledge requirements (step 2)
- Q: Which shelf? — A: storytelling. Existing `storytelling-with-data` lives there; Tufte is the implementation-layer companion. Design shelf was considered but adjacency is weaker.
- Q: Vendor scope — full repo, SKILL.md only, or distill? — A: SKILL.md only (mostly verbatim with workspace frontmatter + footer additions). Per-library `rules/*.md` linked to upstream rather than vendored — keeps maintenance light, credit pointer remains load-bearing.
- Q: Disambiguation with `storytelling-with-data`? — A: Knaflic = upstream framing layer (audience, Big Idea, chart-type choice); Tufte = implementation layer (data-ink, range-frame, per-library config). The pair compose without overlap. Documented in the shelf router's new "Disambiguation: Tufte vs Knaflic" section.
- Q: License compatibility? — A: MIT, compatible with workspace conventions. Copyright preserved in metadata + footer; commit SHA + verification date recorded.
- Q: Branch + merge path? — A: `feat/tufte-data-viz-skill` branch off `develop` (CCP convention from #207); develop-first merge, then promote to main per #208 pattern.

### Contact-point measurements (step 3 — per rules 1-5)
- **data-shape (rule 1):** N/A — no data being consumed by code; this is a markdown vendor commit.
- **config-state (rule 2):** N/A — no runtime config dependency.
- **topology (rule 3):** N/A.
- **plan-shape (rule 4):** N/A.
- **version-resolution (rule 5):** Upstream SHA pinned in metadata + footer + THIRD_PARTY_NOTICES at `ae7ca0d` so consumers can audit drift. Trivial-against-state cross-ref not needed since the measurement was done.

### Surface areas inventoried beyond rules 1-5 (step 4)
- Shelf router file: `skills/shelves/storytelling/SKILL.md` — trigger table format on develop is `[skill:foo]` (NOT relative paths as seen in workspace flat layout); confirmed via Read after a first-edit-then-Read-then-rewrite cycle.
- `AUTHORING.md` frontmatter conventions: `license`, `metadata.source`, `metadata.coverage` (with PARTIAL annotation if scope is partial); verified all three set in new SKILL.md.
- `THIRD_PARTY_NOTICES.md` two-section structure: Upstream sources block (per-upstream metadata) + Per-skill attribution table. Both updated.
- `CHANGELOG.md` — not updated in this commit, matching the precedent of #204 + #207 deferring CHANGELOG to follow-on commits.
- `release/flat` regeneration: triggered on main-push by the build-and-publish workflow; no manual action.

### Hypothesis (step 7 — what the code will implement)
- New file `skills/shelves/storytelling/tufte-data-viz/SKILL.md` with workspace frontmatter (`name`, trigger-phrase-rich `description`, `license: MIT`, `metadata.source` naming Caylent + commit + verification date, `metadata.coverage: PARTIAL` naming exactly what is/isn't vendored).
- Body = upstream SKILL.md body reproduced largely verbatim (rules 1-22, library quick reference, chart-type guidance, color reference, anti-patterns, validation checklist) — verbatim is correct here because the rules ARE the skill; paraphrasing would distort fidelity. License terms preserve verbatim reuse under attribution.
- New footer sections: "When this skill does NOT apply" (boundary conditions naming slide design, cartography, dashboard layout, exploratory analysis) + "Composes with" (storytelling-with-data, writing-claims:5, authoring-against-state:1) + "Source + license" (full attribution including what-was-vendored and what-was-NOT-vendored line items).
- Shelf router `skills/shelves/storytelling/SKILL.md`: add `tufte-data-viz` trigger row + new "Disambiguation: Tufte vs Knaflic" section + Attribution section updated to name caylent/tufte-data-viz alongside ZLStas/skills.
- `THIRD_PARTY_NOTICES.md`: add caylent/tufte-data-viz to Upstream sources (with full metadata block) + add `storytelling | tufte-data-viz | caylent/tufte-data-viz` row to Per-skill attribution table.
- No per-library `rules/*.md` files vendored — explicit link to upstream maintained in the skill's footer.

### Conclusions (steps 5+6)
- All knowledge requirements answered. No deferred questions.
- Inventory was recorded in this artifact (step 5).
- Step 6 (what was NOT measured): no contact-point rule fired (this is markdown vendor work, not code-against-state work), so no measurement was deferred. The version-resolution check (upstream commit SHA) was the one rule-1-to-5 measurement that did apply, and it was performed.
- Hypothesis is achievable given inventory. No conflicts with measured state.

## Peer review (mechanics, correctness, craft floor)

**`_writing-claims-rules.md` writing-claims:1 (grep before declaring complete).** Verified all three deliverables present:
- New SKILL.md: `ls /tmp/ccp-tufte/skills/shelves/storytelling/tufte-data-viz/SKILL.md` → present, 265 lines. PASS.
- Shelf router updated: `grep -n "tufte-data-viz" /tmp/ccp-tufte/skills/shelves/storytelling/SKILL.md` → 3 matches (trigger row, disambiguation, attribution). PASS.
- THIRD_PARTY_NOTICES updated: `grep -n "caylent/tufte-data-viz" /tmp/ccp-tufte/THIRD_PARTY_NOTICES.md` → 2 matches (upstream block + per-skill table). PASS.

**`_writing-claims-rules.md` writing-claims:5 (verify before recommending external resources).** The upstream URL `https://github.com/caylent/tufte-data-viz` and commit SHA were verified via `gh api` calls in the same session. Verification date recorded as 2026-05-23 in metadata. PASS.

**License compliance.** MIT requires preservation of copyright notice + license text. Copyright preserved in: (i) skill frontmatter `metadata.source`, (ii) skill footer `Source + license` block, (iii) THIRD_PARTY_NOTICES upstream-sources block. License notice referenced in all three (upstream LICENSE file linked, not re-vendored — MIT does not require LICENSE re-vendoring for partial-text reuse). PASS.

**Workspace conventions per `AUTHORING.md`.** Frontmatter has required fields (`name`, `description`, `license`, `metadata.source`, `metadata.coverage`). Coverage marked PARTIAL with explicit scope. Body has the upstream's existing structure (rules, library reference, chart-type table, color reference, validation checklist) plus workspace-required "When this skill does NOT apply" + "Composes with" + "Source + license" sections. Depth matches book-skill entries (265 lines, similar to drive-motivation's ~338 and storytelling-with-data's body). PASS.

**Shelf router format consistency.** Used `[skill:foo]` link format matching the existing rows on develop, not the relative-path format from the workspace flat layout. Confirmed by Read after initial Edit attempt failed pattern-match. PASS.

**Disambiguation rigor.** New "Disambiguation: Tufte vs Knaflic" section names the two layers explicitly (upstream framing vs implementation) + gives a workflow example. Mirrors the upstream's own boundary statement ("This skill covers chart generation, not slide/presentation design"). PASS.

**`_writing-prose-rules.md` (shelf voice).** The vendored body is in Caylent's voice (clear, numbered, prescriptive — appropriate for a rules-shelf entry). The workspace-added sections (footer, disambiguation, composes-with) are in the workspace's voice. Voice shift is at section boundaries, not within paragraphs. PASS.

**`_no-ai-fingerprints` / detect-ai-fingerprints.** Workspace-added prose scanned for AI-tone markers — no "delve" / "navigate" / "in conclusion" / Oxford-comma drift. The upstream body is Caylent's, not subject to workspace fingerprint rules. PASS.

## Lead review (approach-fit, blast radius, sequencing)

As skills-shelf author: vendor-mostly-verbatim is approach-fit for an MIT agent skill being absorbed into a sibling shelf system. Distilling would distort the rules (Tufte's rules ARE the skill — paraphrase loses the prescriptive force). Vendoring the per-library `rules/*.md` files was rejected as scope creep — they track library API drift and are better maintained upstream; the workspace's "additional resources" pointer is enough.

Blast radius: new directory only. No existing files removed; shelf router + THIRD_PARTY_NOTICES updates are additive. Will surface to downstream consumers via `release/flat` regeneration after main-merge.

Sequencing assumption: develop-first merge then promote-to-main is the convention (per #204, #207, #208). New feature branch `feat/tufte-data-viz-skill` already created off develop; PR opens against develop.

Approach-fit verdict: PASS. Branch ready for commit + push + PR.

## Evidence-predates-work

Artifact: /tmp/ccp-tufte/.self-review-tufte.md
First-added commit: same-cycle (artifact added alongside work commit, same as the prior two CCP commits this session).
Work commit: to-be-created
Verification: same-cycle is flagged explicitly. The operator's session-spanning license to push commits + the rule-6-applied-to-this-own-work pre-author inventory (above) constitute the pre-work design surface for this change.
